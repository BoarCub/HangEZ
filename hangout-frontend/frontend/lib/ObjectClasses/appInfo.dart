// This class creates an AppInfo object

class AppInfo {
  String username;
  String currentGroupCode;
  var groups;

  // AppInfo class constructor
  AppInfo({this.username, this.groups, this.currentGroupCode});
}